/**
 * Supabase Configuration for PCMB Interactive CET
 *
 * HOW TO CONFIGURE:
 * 1. Go to https://supabase.com and create a free project (or open an existing one).
 * 2. In your project dashboard → Settings → API:
 *    - Copy "Project URL"  → paste as SUPABASE_URL below
 *    - Copy "anon / public" key → paste as SUPABASE_ANON_KEY below
 * 3. Save this file. Do NOT commit it with real values (it is in .gitignore).
 *
 * SECURITY NOTE:
 * The anon key is safe to include in browser code — Supabase was designed for this.
 * Row Level Security (RLS) enforces that users can only access their own data.
 * Never put the service_role key here.
 */

export const SUPABASE_URL = "https://ahxpjyquhxllvsfcapev.supabase.co";
export const SUPABASE_ANON_KEY = "sb_publishable_87Tdgme9iOfRaKlCvShqlQ_DRJ5DGKH";
